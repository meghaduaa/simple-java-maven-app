apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: LoadBalancer
  selector:
    app: myapp
    color: blue    # Initially BLUE is active
  ports:
    - port: 80
      targetPort: 3000

